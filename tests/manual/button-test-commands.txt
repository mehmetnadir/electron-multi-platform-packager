// ============================================
// BUTON TESTİ - CONSOLE KOMUTLARI
// ============================================
// Bu komutları Electron DevTools Console'a yapıştırın

// 1. API KONTROLÜ
// ============================================
console.log("✅ electronAPI:", window.electronAPI);
console.log("✅ openFolder tipi:", typeof window.electronAPI?.openFolder);


// 2. KLASÖRDE GÖSTER TESTİ
// ============================================
// NOT: Path'de tek tırnak olduğu için escape edilmeli veya çift tırnak kullanılmalı

// Yöntem 1: Escape ile
app.openOutputFolder("/Users/nadir/Library/CloudStorage/GoogleDrive-nadir52@gmail.com/Drive\'ım/packager-config/output/Splash Test App_1.0.0_2025-10-27");

// Yöntem 2: Template literal ile (ÖNERİLEN)
app.openOutputFolder(`/Users/nadir/Library/CloudStorage/GoogleDrive-nadir52@gmail.com/Drive'ım/packager-config/output/Splash Test App_1.0.0_2025-10-27`);


// 3. YENİDEN İŞLE TESTİ (Aktif İşlemler)
// ============================================
// Önce bir job ID bulun:
console.log("Aktif işler:", Array.from(app.activeJobs.keys()));

// Sonra retry edin (job ID'yi değiştirin):
app.retryJob("5ac241f2-0437-4c21-b8f6-93643f534290");


// 4. İPTAL ET TESTİ
// ============================================
// Bir paketleme başlatın, sonra:
app.cancelJob("JOB_ID_BURAYA");


// 5. YENİDEN İŞLE TESTİ (Tamamlanan İşler)
// ============================================
// Tamamlanan işlerin index'ini bulun:
console.log("Tamamlanan işler:", app.completedJobs.length);

// İlk işi yeniden işle:
app.reprocessJob(0);


// 6. SİL TESTİ
// ============================================
// Son işi sil:
app.deleteJob(app.completedJobs.length - 1);


// 7. DETAY TESTİ
// ============================================
// İlk tamamlanan işin detayını göster:
app.showJobDetails(app.completedJobs[0].jobId, true);


// ============================================
// BEKLENEN CONSOLE ÇIKTILARI
// ============================================

// Klasörde Göster:
// 📁 Klasör açılıyor: [yol]
// 📁 Klasör açma sonucu: {success: true}

// Yeniden İşle:
// 🔄 İş yeniden başlatılıyor: [jobId]

// İptal Et:
// ❌ İş iptal ediliyor: [jobId]

// Yeniden İşle (Tamamlanan):
// 🔄 İş yeniden işleniyor: [index]

// Sil:
// 🗑️ İş siliniyor: [index]
